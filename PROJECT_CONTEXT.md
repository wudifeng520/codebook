# 项目上下文

> 最后更新：2026-07-28
> 本文件保存跨任务仍然有用的项目状态。具体编码规则以 `AGENTS.md` 为准。

## 项目概况

- 项目名称：本地密码本（`local-vault`）。
- 当前版本：`1.1.0`。
- 主要平台：Windows x64。
- 技术栈：Electron 37、CommonJS JavaScript、原生 HTML/CSS、`node:test`。
- 产品定位：完全离线的本地加密密码管理器。

## 关键设计决定

- 保险库默认位于 `%APPDATA%\local-vault\vault.pvault`，安装版与便携版共用该用户数据目录。
- 使用 scrypt（N=131072、r=8、p=1）派生密钥，并使用 AES-256-GCM 加密。
- 渲染进程保持沙箱、上下文隔离、关闭 Node.js 集成和严格 CSP。
- 保险库格式、加密参数和旧数据兼容性属于安全敏感范围，修改时必须提供迁移和测试。
- 应用默认不添加联网、遥测、远程资源或云同步能力。

## 当前状态

- 当前分支：`main`，跟踪 `origin/main`。
- 已加入 GitHub Release 发布流程：`package.json` 提供 `release` 命令，`tools/publish.ps1` 负责预演、发布和续传，README 已记录使用方法。
- 项目根目录已建立 `AGENTS.md` 和 `PROJECT_CONTEXT.md`，分别保存长期规则和持续项目上下文。

## 最近验证

- `npm.cmd test`：8 项测试通过。
- JavaScript 语法检查：8 个文件通过。
- 验证日期：2026-07-28。

## 当前待办

- 暂无明确待办，按用户后续任务更新。

## 更新规则

- 只记录长期有效的项目事实、重要决定、当前进度、待办和验证结果。
- 状态发生变化后及时更新，删除已经失效的信息。
- 不记录密码、主密钥、访问令牌、Cookie、真实保险库内容或其他敏感数据。
